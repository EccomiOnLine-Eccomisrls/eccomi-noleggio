import CeoPracticePage from "../[id]/page";

export default function PreviewMobilityOnePracticePage() {
  return CeoPracticePage({
    params: Promise.resolve({ id: "PRATICA-PREVIEW-MOBILITY-ONE-001" }),
  });
}
