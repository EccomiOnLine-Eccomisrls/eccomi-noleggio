import { previewPromotionEditableYpsilon } from "../../../lib/server/preview-fixture";
import PreviewOfferView from "../preview-offer-view";

export default function PreviewPartnerOfferTwoPage() {
  return <PreviewOfferView promotion={previewPromotionEditableYpsilon} />;
}
